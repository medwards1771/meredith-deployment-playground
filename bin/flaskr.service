[Unit]
Description=Gunicorn instance to serve flaskr
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/meredith-deploy-playground/
Environment="PATH=/home/ubuntu/meredith-deploy-playground/.venv/bin"
ExecStart=/home/ubuntu/meredith-deploy-playground/.venv/bin/gunicorn --workers 3 --bind 0.0.0.0:3000 -m 007 hello:app
ExecReload=/bin/kill -s HUP $MAINPID

[Install]
WantedBy=multi-user.target